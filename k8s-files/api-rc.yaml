apiVersion: v1
kind: ReplicationController
metadata:
  name: api-controller
spec:
  replicas: 2
  selector:
    app: api
  template:
    metadata:
      name: api
      labels:
        app: api
    spec:
      containers:
        - name: api
          image: muldos/dgs-skeleton:1.2.0
          ports:
            - containerPort: 8080
